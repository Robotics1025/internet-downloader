# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
# Un-ignore the tracked pyinstaller sources (lock + script) within build/.
!build/
!build/pyinstaller/
!build/pyinstaller/binaries.lock
!build/pyinstaller/fetch_binaries.sh

# Node / Electron (future phases)
node_modules/
.vite/
out/

# SQLite local data
*.db
*.db-journal
*.sqlite
*.sqlite3

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Local environment
.env
.env.local

# Claude harness
.claude/

# PyInstaller bundle artifacts and vendored binaries (re-fetched in CI).
build/pyinstaller/binaries/
build/pyinstaller/build/
build/pyinstaller/dist/

# Tauri shell artifacts.
apps/shell/target/
apps/shell/src-tauri-binaries/
